version: '3'

services:
  app:
    image: dbpterraform2/test-node-zipofar
    entrypoint: /bin/bash
    command:
      - "-c"
      - node /app/app/app.js"

x-uffizzi:
  ingress:
    service: app
    port: 8080

  continuous_preview:
    delete_preview_after: 10h
